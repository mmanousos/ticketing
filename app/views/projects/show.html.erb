<h1><%= @project.name %></h1>

<p><%= @project.description %></p>

<%= link_to "Edit", edit_project_path(@project) %> 
<%= link_to "Delete", project_path(@project), method: :delete, 
            data: { confirm: 'Are you sure?' } %> 

<h2>Tickets</h2>

<%= render 'tickets/tickets', tickets: @project.tickets %>