<h1><%= @project.name %></h1>

<p><%= @project.description %></p>

<%= link_to "Edit", edit_project_path(@project) %> 
<%= link_to "Delete", project_path(@project), method: :delete, 
            data: { confirm: 'Are you sure?' } %> 

<h2>Tickets</h2>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Assignee</th>
      <th>Status</th>
      <th>Tags</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>this is my text</td>
      <td>other text</td>
      <td>more sample text</td>
      <td>final text</td>
    </tr>
  </tbody>
</table>